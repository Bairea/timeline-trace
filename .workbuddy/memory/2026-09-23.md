# 2026-09-23

## uv 迁移（timeline-trace）
- 先 `git pull --ff-only`：c4ac4c0 → 01e0314（fix(db) 首页并发加载不再因 SQLite 跨线程而空白）。
- 依赖管理从 requirements.txt + venv 迁到 uv：
  - 新增 `pyproject.toml`：运行时 fastapi/uvicorn[standard]/pydantic/openpyxl 精确 pin；dev 组 pytest/httpx；`requires-python >= 3.11`（代码无 3.11+ 专属语法，本地 venv 3.13）。
  - 生成 `uv.lock`（解析出 28 个包）；`uv sync` 复用现有 .venv，零改动同步。
  - 验证：`uv run pytest -q` 253 passed（唯一 warning 是 starlette 自带弃用提示，与迁移无关）。
  - 移除 `requirements.txt`；README 与 deploy/README §2 改为 uv 命令（服务器 `uv sync --frozen --no-dev`；`.venv` 路径不变，systemd ExecStart 无需改）。
  - 迁移前备份：`backups/pre-uv-migration-2026-09-23.zip`（requirements.txt + 两份 README）。
- 提交 4e88e35（chore），**未推送**，等用户过目后 push。
- 机器级配置：`%APPDATA%\uv\uv.toml` 设阿里云 PyPI 为默认索引（用户偏好的落地）。
